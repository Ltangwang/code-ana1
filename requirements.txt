# code-ana1: bi-encoder + Ollama + cloud code search

# Core async and HTTP (asyncio is in the Python 3 stdlib; do not install PyPI "asyncio")
aiohttp==3.9.1
httpx==0.25.2

# Cloud LLM clients
openai==1.6.1

# Data validation and schemas
pydantic==2.5.3

# Configuration
pyyaml==6.0.1

# Logging
structlog==23.2.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-mock==3.12.0
pytest-cov==4.1.0

# Code quality
black==23.12.1
ruff==0.1.8
mypy==1.7.1

# Utilities
python-dotenv==1.0.0
tenacity==8.2.3

# PyTorch (install CUDA build from pytorch.org if needed)
torch>=2.1.0

# UniXcoder / eval (evaluate_code_search, csn_retriever, train_unixcoder_csn*)
transformers>=4.36.0
datasets>=2.14.0
tqdm>=4.65.0
numpy>=1.24.0
# Cross-encoder rerank (evaluate_code_search / csn_ce_rerank)
sentence-transformers>=3.0.0
